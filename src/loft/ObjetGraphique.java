package loft;import java.awt.*;import java.awt.event.*;import javax.swing.*;/** * Classe definissant un objet graphique a tracer par la classe Affichage. *  * @author Gaetan, Marlene */abstract class ObjetGraphique {        // pour distinguer plusieurs ObjetGraphiques sur une meme Case    protected int positionRelative = 0;            public ObjetGraphique() {        // do nothing    }        public void setPositionRelative(int pos) {        this.positionRelative = pos;    }            /**     * Dessine l'ObjetGraphique     * @param g      */    public void dessiner(Graphics g) {        colorer(g);        tracer(g);    }        public void colorer(Graphics g){        g.setColor(Color.BLACK);    }        abstract public void tracer(Graphics g);            public void ligne(Graphics g, int x1, int y1, int x2, int y2) {        g.drawLine(x1, y1, x2, y2);    }        public void trajet(Graphics g, int xA, int yA, int xB, int yB) {        g.setColor(Color.RED);        g.drawLine(xA, yA, xB, yB);        int taillePtB = 4;        g.fillRect(xB - taillePtB/2, yB - taillePtB/2, taillePtB, taillePtB);    }        public void rectangle(Graphics g, int x, int y, int width, int height) {        // rectangle avec contour noir        g.fillRect(x, y, width, height);        g.setColor(Color.BLACK);        g.drawRect(x, y, width, height);    }        public void cercle(Graphics g, int x, int y, int diameter) {        // cercle avec contour noir        g.fillOval(x, y, diameter, diameter);        g.setColor(Color.BLACK);        g.drawOval(x, y, diameter, diameter);    }    }